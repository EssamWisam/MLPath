'''
This should include functions that preprocess, clean and transform the data. It should have a function that returns
x_train, x_val and y_train, y_val,.. x_test, y_test (only for scoring) that are suitable for feature extraction.
Consider adding the capability to save the data as it may impede your productivity if this takes a long time.
'''